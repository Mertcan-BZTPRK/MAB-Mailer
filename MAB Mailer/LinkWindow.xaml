<Window x:Class="MAB_Mailer.LinkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Link Ekle" Height="220" Width="350"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent" Icon="/icon.png">

    <Border Background="{DynamicResource CardBackground}" CornerRadius="15" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.5"/>
        </Border.Effect>
        <Grid Margin="20">
            <StackPanel>
                <TextBlock Text="Görünen Metin:" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,5"/>
                <TextBox x:Name="txtDisplay" Height="30" Padding="5"/>

                <TextBlock Text="Link Adresi (URL):" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,10,0,5"/>
                <TextBox x:Name="txtUrl" Height="30" Padding="5" Text="https://"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="İptal" Width="70" Height="30" Click="BtnCancel_Click" Style="{StaticResource RedGlassButton}" Margin="0,0,10,0"/>
                    <Button Content="Link Oluştur" Width="100" Height="30" Click="BtnOk_Click" Style="{StaticResource BaseGlassButton}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>